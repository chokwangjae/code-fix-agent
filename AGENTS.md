# 작성 품질 규칙

문서, 코드 주석, 커밋 메시지를 새로 쓰거나 수정할 때 다음 순서를 지킨다.

1. `.agents/skills/stop-slop/SKILL.md`를 읽고 군더더기, 상투적인 AI 문장 구조, 모호한 표현을 걷어낸다.
2. `.agents/skills/humanize-korean/SKILL.md`와 필요한 참조 파일을 읽고 번역투와 기계적인 문장 패턴을 보수적으로 다듬는다.
3. 명령어, 코드, 식별자, 파일 경로, 설정 키, 수치, URL의 의미와 값을 바꾸지 않는다.
4. 코드 블록과 인라인 코드는 문체 윤문 대상에서 제외한다.
5. 코드 주석은 동작을 되풀이하지 않고 제약, 이유, 주의사항을 설명한다.

한국어가 아닌 글에는 `stop-slop`을 적용하고, `humanize-korean`은 적용 가능 여부만 확인한다. 맞춤법 수정처럼 범위가 작은 작업은 필요한 부분만 고친다.

# 저장소 작업 범위

- 기본 작업 범위는 `code-fix-agent` 저장소 내부로 제한한다.
- 외부 저장소는 연동 계약, 대상 프로젝트 규칙과 하네스를 확인할 때 읽기 전용으로만 조사한다.
- 외부 저장소의 파일, 설정, 상태 DB, branch를 수정하거나 외부 저장소에서 commit, push, PR을 만들지 않는다.
- 구현에 외부 저장소 변경이 필요하면 필요성, 대상 저장소·파일, 변경 내용, 예상 영향과 검증 방법을 먼저 설명하고 명시적 승인을 요청한다.
- `code-fix-agent`의 런타임 기능이 만드는 대상 저장소 worktree, branch와 PR은 제품 동작에 해당한다. 개발·검증 과정에서는 사용자가 실제 실행을 승인하기 전까지 이 기능을 외부 저장소에 실행하지 않는다.

# 커밋 메시지 규칙

- 커밋 메시지는 한국어로 작성.
- 제목과 본문의 각 항목은 명사형으로 종결.
- 서로 다른 기능의 변경은 합치지 않고 기능별로 분리해 커밋.
- 아래 구조를 그대로 사용.

```text
feat (<type>): title

무엇:
왜:
검증:
동기화:
```
